import { tab } from '@testing-library/user-event/dist/tab'
import React from 'react'

function Tasks() {
  return (
    <div>
        if (Task == 0) {
            tab = Tasks
        }
      
    </div>
  )
}

export default Tasks
